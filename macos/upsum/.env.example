# upsum 환경 변수 설정 예제
# 이 파일을 .env 파일로 복사하고 실제 값으로 채워주세요: cp .env.example .env

# ==================== Google Gemini API ====================
# Google AI Studio에서 발급받은 Gemini API 키
# 발급 방법: https://aistudio.google.com/app/apikey
GEMINI_API_KEY=your_gemini_api_key_here

# ==================== SMTP 이메일 설정 ====================
# 이메일 발송을 위한 SMTP 서버 정보

# SMTP 서버 주소
# - Gmail: smtp.gmail.com
# - Synology MailPlus: mail.your-domain.com (또는 NAS IP)
# - Office 365: smtp.office365.com
SMTP_HOST=smtp.gmail.com

# SMTP 포트
# 포트 선택 가이드:
#   - 465: SSL/TLS 암호화 포트 (권장, Synology MailPlus 일반적 설정)
#   - 587: TLS 암호화 포트 (Gmail 권장)
#   - 25: 암호화 없음 (비권장, 스팸 필터링 가능성)
#
# Synology MailPlus의 경우:
#   - SSL(465)을 먼저 시도하고, 실패 시 TLS(587) 사용
#   - NAS 내부 IP 사용: 예) 192.168.1.100:465
#   - 도메인 사용: 예) mail.example.com:465
SMTP_PORT=587

# SMTP 인증 사용자명 (보통 이메일 주소)
# - Gmail: your_email@gmail.com
# - Synology MailPlus: username@yourdomain.com 또는 username
SMTP_USER=your_email@gmail.com

# SMTP 비밀번호
# Gmail 사용자의 경우: 2단계 인증 활성화 후 "앱 비밀번호"를 생성하여 사용
# 앱 비밀번호 생성: https://myaccount.google.com/apppasswords
# 
# Synology MailPlus의 경우: MailPlus에서 설정한 계정 비밀번호 사용
SMTP_PASSWORD=your_app_password_here

# ==================== 이메일 발송/수신 주소 ====================
# 발신자 이메일 주소 (선택사항, 미설정 시 SMTP_USER 사용)
MAIL_FROM=your_email@gmail.com

# 수신자 이메일 주소 (보고서를 받을 주소)
MAIL_TO=recipient@example.com

# ==================== 참고 사항 ====================
# 1. Gmail 사용자
#    - 2단계 인증 활성화 필요
#    - "앱 비밀번호"를 SMTP_PASSWORD로 사용
#    - 포트: 587 (TLS) 권장
#
# 2. Synology MailPlus 사용자
#    - MailPlus 설정에서 발급한 SMTP 서버 주소/포트 확인
#    - 포트: 465 (SSL) 일반적, 또는 587 (TLS)
#    - 내부 NAS IP 또는 도메인 사용
#    - 예시:
#      SMTP_HOST=192.168.1.100  (또는 mail.example.com)
#      SMTP_PORT=465
#      SMTP_USER=user@example.com
#      SMTP_PASSWORD=mailplus_password
#
# 3. 이메일 전송 실패 시
#    - 호스트명/IP와 포트 확인
#    - 사용자명과 비밀번호 확인
#    - 방화벽/네트워크 설정 확인
#    - upsum --dry-run으로 테스트
#
# 4. .env 파일은 .gitignore에 포함되어 있어 git에 커밋되지 않습니다
# 5. 모든 값은 따옴표 없이 입력하세요
